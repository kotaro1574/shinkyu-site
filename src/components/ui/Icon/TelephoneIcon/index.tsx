import { BaseIcon, IconProps } from '@src/components/ui/Icon/BaseIcon'
import { BsFillTelephoneFill } from 'react-icons/bs'

export const TelephoneIcon = (props: IconProps) => {
  return <BaseIcon icon={BsFillTelephoneFill} {...props} />
}
