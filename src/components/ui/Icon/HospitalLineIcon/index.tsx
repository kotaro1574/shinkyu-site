import { BaseIcon, IconProps } from '@src/components/ui/Icon/BaseIcon'
import { RiHospitalLine } from 'react-icons/ri'

export const HospitalLine = (props: IconProps) => {
  return <BaseIcon icon={RiHospitalLine} {...props} />
}
