import { BaseIcon, IconProps } from '@src/components/ui/Icon/BaseIcon'
import { BsSignpost2 } from 'react-icons/bs'

export const SignpostIcon = (props: IconProps) => {
  return <BaseIcon icon={BsSignpost2} {...props} />
}
