import { BaseIcon, IconProps } from '@src/components/ui/Icon/BaseIcon'
import { AiFillCaretRight } from 'react-icons/ai'

export const CaretRightIcon = (props: IconProps) => {
  return <BaseIcon icon={AiFillCaretRight} {...props} />
}
