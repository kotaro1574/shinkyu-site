import { BaseIcon, IconProps } from '@src/components/ui/Icon/BaseIcon'
import { BsLine } from 'react-icons/bs'

export const LineIcon = (props: IconProps) => {
  return <BaseIcon icon={BsLine} {...props} />
}
