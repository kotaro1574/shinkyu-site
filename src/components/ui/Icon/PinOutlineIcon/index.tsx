import { BaseIcon, IconProps } from '@src/components/ui/Icon/BaseIcon'
import { IoPinOutline } from 'react-icons/io5'

export const PinOutlineIcon = (props: IconProps) => {
  return <BaseIcon icon={IoPinOutline} {...props} />
}
